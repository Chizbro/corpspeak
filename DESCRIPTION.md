This is CorpSpeak (short for Corporate Speak). It is a multi-user chatroom where every user's message is first fed through an LLM that translates it into mindless corporate jargon before posting to the room. The app is hosted on Netlify; messages and realtime updates use Supabase (Postgres + Realtime), with Google Gemini performing the translation in a SvelteKit API route.